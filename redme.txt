"mfrc522.py" is the library or package for accessing RFID module (rc522). We need to add this on pi pico first.

Sometime we need to run Reset.py on pi pico to reset the RFID module. Then we can run actual program.
